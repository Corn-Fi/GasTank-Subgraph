{
  "name": "trades",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ kcorkscrew/trades",
    "create-local": "graph create --node http://localhost:8020/ kcorkscrew/trades",
    "remove-local": "graph remove --node http://localhost:8020/ kcorkscrew/trades",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kcorkscrew/trades"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
